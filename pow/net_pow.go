package pow

// TODO(teawithsand): Network-based PoW, which require client to have specific upload/download speed.
// Implement that with sharding support(require one sender, but there may be multiple receivers of different data).
